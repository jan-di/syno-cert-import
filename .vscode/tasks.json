{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Docker Image",
            "type": "shell",
            "command": "docker build . -t syno-cert-import",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}